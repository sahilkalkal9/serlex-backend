name: Test Backend Workflow

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test workflow trigger
        run: |
          echo "GitHub Actions trigger is working"
          echo "Branch: ${{ github.ref }}"
          echo "Commit: ${{ github.sha }}"